name: habitr_models
description: Common models for Habitr and its server-side functions.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  gql: ^0.13.1-0
  gql_code_builder: ^0.3.0-0
  json_annotation: ^4.3.0

dev_dependencies:
  build_runner: ^2.0.4
  gql_build: ^0.3.0-0
  json_serializable: ^6.0.0
  amplify_lints: ^1.0.0
  test: ^1.16.0
